services:
  - type: web
    name: ollama-fastapi
    env: docker
    plan: free
    docker:
      dockerfilePath: ./Dockerfile
    envVars:
      - key: OLLAMA_MODELS
        value: /root/.ollama/models
    healthCheckPath: /health
